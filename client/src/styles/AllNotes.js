import styled from '@emotion/styled'

const Wrapper = styled.section`
  margin-top: 50px;
  .notes-container {
    flex-direction: row;
    flex-wrap: wrap;
    row-gap: 1.2em;
    column-gap: 1.2em;
  }
  .paper {
    padding: 15px;
    width: 280px;
    overflow: hidden;
  }
`

export default Wrapper
